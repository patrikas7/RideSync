import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { marginTop: 16, flex: 1 },
  multiline: { marginTop: 24 },
  button: { marginBottom: 32 },
});

export default styles;
