import { StyleSheet } from "react-native";
import Colors from "../../Constants/colors";

const MapLocationSearchStyles = StyleSheet.create({
  inputContainer: {
    marginTop: 16,
  },
  buttonsContainer: {
    marginBottom: 32,
    marginHorizontal: 0,
    flexDirection: "row",
  },
  button: {
    flex: 1,
  },
});

export default MapLocationSearchStyles;
